apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flowise-storage
  labels:
    app: flowise
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: gp2  # AWS EBS gp2
  resources:
    requests:
      storage: 10Gi
